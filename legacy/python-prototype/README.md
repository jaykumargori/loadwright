# Python Prototype

This directory contains the original experimental Python implementation that informed Loadwright's direction.

It is kept as reference material only. The maintained OSS implementation is the Go CLI in:

- `cmd/loadwright`
- `internal/`

New features should be built in Go unless there is a deliberate migration plan documented in `ROADMAP.md`.

